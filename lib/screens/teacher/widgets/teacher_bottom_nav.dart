// // Thin compatibility shim — delegates to the shared AppBottomNavBar component.
// // New code should import app_bottom_nav_bar.dart directly.
// export 'package:loringo_app/components/app_bottom_nav_bar.dart'
//     show AppBottomNavBar, AppNavItem;

// // Keep legacy type aliases so existing teacher code compiles without changes.
// import 'package:loringo_app/components/app_bottom_nav_bar.dart';

// typedef TeacherNavItem = AppNavItem;
// typedef TeacherBottomNav = AppBottomNavBar;
